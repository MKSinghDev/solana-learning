'use client';

import { ReactNode } from 'react';

import '~/lib/utils/string-extension';

const NativeExtensionsProvider = ({ children }: { children: ReactNode }) => children;

export default NativeExtensionsProvider;
